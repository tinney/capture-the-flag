%h3 Players
%div= link_to 'New Player', new_team_player_path(@team)
%table.table
  %thead
    %th Location
    %th Has Flag
    %th Has Peg
    %th Moves
  %tbody
    - @players.each do |player|
      %tr
        %td== #{player.x}, #{player.y}
        %td= player.has_flag?
        %td= player.has_peg?
        %td= link_to "moves", new_player_move_path(player)